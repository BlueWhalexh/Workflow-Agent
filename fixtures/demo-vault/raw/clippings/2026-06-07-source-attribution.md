# Source attribution examples

This research note belongs with Cobalt Notes, but frontmatter was never added.

Example rewrite rule:
- If an agent adds a project link based on a note paragraph, the changeset source must include the note ref and range.
- The UI may show a short citation, but the public event should not expose server-local paths.

中文例子:
把 "frontmatter required fields" 写入 MOC 时, source 应指向研究笔记原句,不能只写 "LLM inferred".

Related maybe: [[raw/clippings/2026-06-03-frontmatter-study]]
